---
title: "Get Identity Params"
api: GET /sonr-io/sonr/identity/params
description: "Get the parameters for the identity registry"
---

<ResponseExample>
```json Response (200)
{
    "params": {
        "did_base_context": "string",
        "did_implementation_context": "string",
        "ipfs_gateway": "string",
        "ipfs_api": "string"
    }
}
```

```json Error
{
  "code": 0,
  "message": "string",
  "details": [
    {
      "@type": "string"
    }
  ]
}
```

</ResponseExample>
